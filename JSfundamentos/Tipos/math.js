const raio = 5.6
const area = Math.PI * Math.pow(raio, 2)//pow de power: defini um número elevado a outro (numero1, numero2)// são parametros

console.log(area.toFixed(2))
console.log(typeof Math)// aqui math não é uma função e sim um objeto